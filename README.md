# Hyfens Scoop Bucket

Install the Hyfens CLI on Windows:

```powershell
scoop bucket add hyfens https://github.com/hyfens-hq/scoop-bucket
scoop install hyfens
hyfens --version
```

The manifest points to the immutable `v0.1.0` GitHub Release archives and
uses their published SHA-256 checksums.
